node
{
    stage("checkout scm")
    {
        echo("check the scm")
        
        
        echo("checked the scm")
        
    }
    stage ("build")
    {
        echo("build the  code")
    }
    stage ("test")
    {
       
        echo("test the code")
    }
}

